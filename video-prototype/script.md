# 第1回 台本 —「時間術の本15冊の"紹介文"を分析したら、共通点がゼロだった」

チャンネル: **本のデータ研究所**
形式: 漫画風ポップアニメ + 女の子2人の掛け合い + VOICEVOX音声(顔出し・声出しなし)

**数値はすべて実データ。** 集計手順は `../analysis/START_HERE.md`、
抽出結果と名寄せ表は `../analysis/recovered/` にある。

---

## 使用している数値(すべて実測)

| 項目 | 値 |
|---|---|
| 分析対象 | 15冊(Kindleストア 時間術カテゴリの売れ筋ランキング上位から機械的に選定) |
| 使ったデータ | Amazonの商品説明(紹介文)のみ。本文・目次は不使用 |
| 抽出の採用基準 | 同じ処理を2回実行し、**両方に出た主張だけ**を採用 |
| 採用された主張 | 延べ **21個**(1冊あたり1.4個) |
| 名寄せ後 | **13種類** |
| 最多の主張 | 「時間の使い方を見直す」 **3/15** |
| 2〜7位 | 6種類がすべて **2/15** |
| **15冊すべてに共通** | **0個** |
| 対照実験(b01) | 紹介文のみ **1個** / 目次を足すと **12個**、重複ゼロ |

延べ21個の内訳: 1冊あたり b01〜b15 = 1,1,1,1,2,1,1,1,2,1,2,2,1,3,1 → 合計21。
名寄せは61表現→11グループ(統合しなかったものを含めて全13種類)。

---

## 構成(全22シーン / 128行 / VOICEVOX版で約8分10秒)

| # | セクション | 内容 | 演出 |
|---:|---|---|---|
| 1 | OPフック | 15冊の紹介文だけを集めた | 大数字 15 |
| 2 | OP衝撃 | **共通していた主張は0個** | 大数字 0 + 集中線 |
| 3 | 予告 | 今日わかる3つ | 箇条書き |
| 4 | 検証: 選書 | ランキングから機械的に。主観を入れない | 箇条書き |
| 5 | 検証: データ | 紹介文だけ。本文は使わない | 箇条書き |
| 6 | 検証: 反論先回り | 「AIの分析、信用できるの?」→2回実行・両方一致のみ | 箇条書き |
| 7 | パイプライン | 延べ21個 → 13種類 | 箇条書き |
| 8 | 1.4個の意味 | 200字読んでも1〜2個しか分からない | 大数字 1.4 |
| 9 | POINT1 | ランキング上位(3/15, 2/15, 2/15) | 横棒グラフ |
| 10 | POINT1-b | 1位「時間の使い方を見直す」に中身がない | 箇条書き |
| 11 | POINT1-c | 2位以下も全部2冊。横並び | 横棒グラフ(4本) |
| 12 | **POINT2** | **本がバラバラだからではない** | 集中線 + 箇条書き |
| 13 | POINT2-b | 紹介文は「説明」ではなく「宣伝」 | 箇条書き |
| 14 | POINT2-c | 悪いのは出版社ではない。期待する場所が違った | 箇条書き |
| 15 | **POINT3** | **同じ本を目次つきで分析したら12個** | 大数字 12 + 集中線 |
| 16 | POINT3-b | 目次から出た具体策の実物 | 箇条書き |
| 17 | POINT3-c | 1 vs 12 を並べて見せる | 横棒グラフ |
| 18 | 結論 | **紹介文で本を選ぶな** | 集中線 + 箇条書き |
| 19 | 今日からできること | 目次の見方3つ(3分で確認できる) | 箇条書き |
| 20 | 限界の開示 | 15冊は少ない / 個人出版中心 / 紹介文の長さに差 | 箇条書き |
| 21 | まとめ | 結論3つ + アクション + コメント誘導 | 箇条書き |
| 22 | 次回予告 | 次は15冊ぜんぶの目次で同じ分析 | 箇条書き |

台本の全文は `build_video.py` の `SCENES` に記載。

### 動画の核になる主張

> 15冊の紹介文に共通点はなかった。でもそれは本がバラバラだからではない。
> **紹介文に中身が書かれていないから**だ。
> 同じ本でも、目次を見れば12個の具体策が出てくる。
> **紹介文で本を選ぶな。目次を見ろ。**

当初は「どの本も同じことを言っている」という仮説だったが、集計したら逆の結果が出た。
**仮説が外れた過程をそのまま動画にしている**のがこの回の価値。

---

## キャラクターと音声

| 役 | キャラ | 見た目 | VOICEVOX 話者 | 口調 |
|---|---|---|---|---|
| 後輩・ツッコミ | ノア | ミント髪ツインテール + 黄色リボン | 春日部つむぎ (speaker 8) | 〜です / 〜ですか? |
| 先輩・解説 | メイ先生 | ローズ髪ロング + メガネ | 四国めたん (speaker 2) | 〜だわ / 〜よ |

話者は `SPEAKERS` で変更可能(3=ずんだもん / 13=青山龍星 / 14=冥鳴ひまり など)。

### VOICEVOX 利用上の注意

- 無料・商用利用可だが、**キャラクターごとにクレジット表記が必須**。
  概要欄に `VOICEVOX:春日部つむぎ` `VOICEVOX:四国めたん` と記載する。
- **キャラ絵はオリジナルの簡易イラスト**。VOICEVOXの公式立ち絵ではない。
- 合成音声を使うので、YouTube Studio の**AI・合成コンテンツの開示設定をONにする**。

---

## 使い方

```bash
# VOICEVOXアプリを起動してから実行すると、キャラ音声になる
python3 build_video.py ./build

# 必要パッケージ: brew install ffmpeg / pip3 install pillow numpy
```

出力: `build/sample_video.mp4` / `build/thumbnail.png` / `build/narration.wav` /
`build/chapters.txt`(概要欄に貼るチャプター)

### 尺の調整

VOICEVOXは読み上げが速いので、初回ビルドは7分41秒だった。
**8分未満だとミッドロール広告を入れられない**ため、次の2つで8分を超えるようにしてある。

- `speedScale` を 1.05 → **1.0**(数字の多い動画なので、速さより聞き取りやすさを優先)
- `LINE_PAUSE` を 0.34 → **0.40** 秒、`SCENE_PAUSE` を 0.8 → **0.9** 秒

尺を変えたいときはこの3つをいじる。

チャプターは音声の実測長から自動生成されるので、手で測る必要はない。
話者や台本を変えて尺が変わっても、ビルドし直せば自動で追従する。
区切り位置は `SCENES` の `chapter` キーで決まる。

---

## タイトル案(A/Bテスト用)

1. 時間術の本15冊の「紹介文」を分析したら、共通点がゼロだった
2. Amazonの紹介文で本を選ぶのをやめた方がいい理由【15冊分析】
3. 同じ本なのに、紹介文からは1個・目次からは12個だった話

## サムネ案

`build_video.py` が `build/thumbnail.png` を出す
(「共通点**ゼロ**だった」+ サブ帯「紹介文で本を選ぶな」+ 2キャラ)。

### ただし第1回はこれを使えない

カスタムサムネイルには**チャンネルの電話番号認証が必要**で、
同じ番号は他チャンネルで使用済みのため認証できない。
番号の使用枠は約1年で戻ることがあるので、認証できたら後から差し替える
(サムネイルは公開後いつでも変更でき、差し替えても再生回数はリセットされない)。

### 認証できるまでは自動サムネイルで出す

YouTubeは動画のおよそ **25% / 50% / 75%** の位置から3枚の候補を作る。
本編は全編が大きな文字のスライドなので、どの候補も文字は読める。
行数から見た各候補の中身は次のとおり(実尺は行の長さで多少ずれる)。

| 候補 | 位置 | 当たるシーン | 見え方 |
|---|---|---|---|
| 1枚目 | 約25% | #6「AIの分析、信用できるの?」 | 箇条書き。弱い |
| 2枚目 | 約50% | #12「本がバラバラだから、ではない」 | 集中線あり。中くらい |
| 3枚目 | 約75% | #17〜18「紹介文からは1個、目次からは12個」/「紹介文で本を選ぶな」 | **一番強い。これを選ぶ** |

自動サムネは弱いので、**第1回のクリック率はタイトルが担う**。
数字と結論を前に置いたタイトルを使うこと。

## 概要欄テンプレ

```
本を「読む前にわかること」を増やすチャンネル「本のデータ研究所」です。

今回は時間術の本15冊の「Amazon紹介文」だけを集めて、
何を勧めている本なのかをAIに抽出させ、共通点を数えました。

【分析方法】
・Kindleストアの時間術カテゴリ売れ筋ランキングから機械的に選定
・ジャンル違いと著者の重複を除外して15冊
・使用したのはAmazonの商品説明のみ(本文は不使用)
・抽出は2回実行し、両方に出た主張のみ採用
・登場冊数のカウントはスクリプトで機械的に集計

【結果】
・採用された主張: 延べ21個 → 名寄せ後13種類
・最多「時間の使い方を見直す」3/15
・15冊すべてに共通していた主張: 0個
・対照実験: 同じ本でも目次を足すと1個→12個(重複ゼロ)

※本動画は書籍の要約ではなく、公開情報の集計と解釈に基づく解説です。
　各書籍の中身は目次から先にあります。ぜひ現物をご確認ください。

▼分析した15冊のリスト
→ (リンク)

▼使用したスクリプト一式
→ (リンク)

音声: VOICEVOX:春日部つむぎ / VOICEVOX:四国めたん
※音声合成・AI編集ツールを使用しています(AI開示設定ON)
```

---

## 本番投稿前チェックリスト

- [x] 数値を実測値にした(`analysis/` で集計済み)
- [x] **VOICEVOXを起動して音声を差し替えた**
- [x] 差し替え後、`WATERMARK` を空文字にした
- [ ] 分析した15冊のリストを概要欄用に用意した
- [ ] `build/chapters.txt` の中身を概要欄に貼った(自動生成されるので測り直し不要)
- [ ] VOICEVOXのクレジットを概要欄に記載した
- [ ] YouTube Studio のAI・合成コンテンツ開示設定をONにした
- [ ] サムネイルは自動生成の3候補から3枚目(約75%地点)を選んだ
      ※電話番号認証ができるようになったら `build/thumbnail.png` に差し替える
- [ ] Shorts切り出し候補を決めた(「紹介文からは1個、目次からは12個」が最有力)

## 未実装

- 効果音・BGM ※フリー音源の規約確認を忘れずに
- キャラの表情差分(驚き・呆れ)
- Shorts用の縦型(1080x1920)書き出し
